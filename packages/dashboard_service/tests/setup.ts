import { jest } from '@jest/globals';

beforeAll(() => {
    console.log('Configuration des tests globaux pour dashboard service');
});

afterAll(() => {
    console.log('Nettoyage final des tests pour dashboard service');
});